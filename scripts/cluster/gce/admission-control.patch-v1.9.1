--- config-default.sh.orig	2018-02-15 17:45:44.942652340 -0800
+++ config-default.sh.patched	2018-02-15 18:13:19.861566822 -0800
@@ -299,6 +299,14 @@
 # Admission Controllers to invoke prior to persisting objects in cluster
 ADMISSION_CONTROL=Initializers,NamespaceLifecycle,LimitRanger,ServiceAccount,PersistentVolumeLabel,DefaultStorageClass,PersistentVolumeClaimResize,DefaultTolerationSeconds,NodeRestriction,Priority,PVCProtection
 
+# Allows Stolos to inject additional admission controllers beyond ones that
+# are allowed in the original script.
+STOLOS_ADMISSION_CONTROL=${STOLOS_ADMISSION_CONTROL:-}
+if [[ ${STOLOS_ADMISSION_CONTROL} != "" ]]; then
+  echo "INFO: STOLOS_ADMISSION_CONTROL=${STOLOS_ADMISSION_CONTROL}"
+  ADMISSION_CONTROL="${ADMISSION_CONTROL},${STOLOS_ADMISSION_CONTROL}"
+fi
+
 if [[ "${ENABLE_POD_SECURITY_POLICY:-}" == "true" ]]; then
   ADMISSION_CONTROL="${ADMISSION_CONTROL},PodSecurityPolicy"
 fi
