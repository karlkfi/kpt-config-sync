apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  creationTimestamp: null
  labels:
    bespin.dev/system: "true"
  name: folders.bespin.dev
spec:
  group: bespin.dev
  names:
    kind: Folder
    plural: folders
  scope: Cluster
  # TODO(b/118383540): This is commented out because a bug in GKE 1.10 Alpha is rejecting
  # the validation schema even though it is valid.
  #validation:
    #openAPIV3Schema:
      #properties:
        #apiVersion:
          #type: string
        #kind:
          #type: string
        #metadata:
          #type: object
        #spec:
          #properties:
            #displayName:
              #pattern: '[a-zA-Z\d][\w_ \-]{3,27}[\w\d]?'
              #type: string
            #id:
              #format: int64
              #minimum: 1
              #type: integer
            #importDetails:
              #properties:
                #time:
                  #format: date-time
                  #type: string
                #token:
                  #pattern: ^\w{40}$
                  #type: string
              #required:
              #- token
              #- time
              #type: object
            #parentReference:
              #properties:
                #kind:
                  #pattern: ^(Organization|Folder)$
                  #type: string
                #name:
                  #type: string
              #required:
              #- kind
              #- name
              #type: object
          #required:
          #- displayName
          #- importDetails
          #type: object
        #status:
          #properties:
            #id:
              #format: int64
              #type: integer
            #syncDetails:
              #properties:
                #error:
                  #type: string
                #time:
                  #format: date-time
                  #type: string
                #token:
                  #pattern: ^\w{40}$
                  #type: string
              #type: object
          #type: object
      #required:
      #- spec
      #type: object
  version: v1
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
