apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.2.5
  name: reposyncs.configmanagement.gke.io
spec:
  group: configmanagement.gke.io
  names:
    kind: RepoSync
    listKind: RepoSyncList
    plural: reposyncs
    singular: reposync
  scope: Namespaced
  validation:
    openAPIV3Schema:
      description: RepoSync is the Schema for the reposyncs API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: RepoSyncSpec defines the desired state of RepoSync
          properties:
            foo:
              description: Foo is an example field of RepoSync. Edit RepoSync_types.go
                to remove/update
              type: string
          type: object
        status:
          description: RepoSyncStatus defines the observed state of RepoSync
          type: object
      type: object
  version: v1
  versions:
  - name: v1
    served: true
    storage: true
