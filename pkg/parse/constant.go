package parse

const (
	// RootSyncReconcilerType defines the type for a root reconciler in format 'root-sync'
	RootSyncReconcilerType = "root-sync"
	// RepoSyncReconcilerType defines the type for a namespace reconciler in format 'repo-sync'
	RepoSyncReconcilerType = "repo-sync"
)
