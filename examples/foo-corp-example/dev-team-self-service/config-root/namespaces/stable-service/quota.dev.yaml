apiVersion: v1
kind: ResourceQuota
metadata:
  name: dev-stable-quota 
  annotations:
      configmanagement.gke.io/cluster-selector: selector-env-dev
spec:
  hard:
    pods: "4"
    requests.cpu: "1"
    requests.memory: 1Gi
    limits.cpu: "2"
    limits.memory: 2Gi
